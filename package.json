{
    "name": "news-translation-system",
    "private": true,
    "scripts": {
        "dev": "concurrently --raw \"npm run dev --prefix client\" \"npm run dev --prefix server\""
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}